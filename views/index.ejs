<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Livraria</title>
</head>

<body class="container">
    <div class="wrapper-form">
        <h1>Buscar Livros</h1>
        <form class="busca" action="/buscar" method="GET">
            <div class="form-group">
                <label for="titulo">Título:</label>
                <input type="text" id="titulo" name="titulo" value="<%= typeof titulo !== 'undefined' ? titulo : '' %>">
            </div>
            <div class="form-group">
                <label for="ano">Ano:</label>
                <input type="number" id="ano" name="ano" value="<%= typeof ano !== 'undefined' ? ano : '' %>">
            </div>
            <button type="submit">Buscar</button>
            <a href="/" class="btn-clear">Limpar busca</a>
        </form>
    </div>

    <div class="resultados">
        <% let contador=1; %>
            <% if (resultados && resultados.length> 0) { %>
                <h2>Resultados de Busca</h2>
                <ul>
                    <% resultados.forEach((livro, index)=> { %>
                        <li class="cor-<%= (index % 4) + 1 %>">
                            <%= contador++ %> - <%= livro.titulo %> - <%= livro.autor %> (<%= livro.ano %>)
                        </li>
                        <% }) %>
                </ul>
                <% } else if (resultados && resultados.length===0) { %>
                    <p>Nenhum livro encontrado.</p>
                    <% } else { %>
                        <p>Faça uma nova busca agora mesmo.</p>
                        <% } %>
    </div>

</body>

</html>
